[package]
name = "aster-config"
version = "0.1.0"
edition = "2024"

[dependencies]
aster-core = { path = "../aster-core" }
serde = { version = "1.0", features = ["derive"] }
ron = "0.8"
thiserror = "1.0"

[dev-dependencies]
tempfile = { workspace = true }
